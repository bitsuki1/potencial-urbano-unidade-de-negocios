# Correlatos — fora do foco IPTU/TDC (preservados, não descartados)

> **Decisão do MOU (2026-06-21):** *"se não prestam pro projeto, tira daí, não é foco esses temas — mas podem ser alguma coisa correlata."*
> Estes itens **saíram do corpus ativo** (não contam no MANIFESTO, `scripts/consolidar.py` ignora `_correlatos/`),
> mas **não foram apagados** (D24/RO-09 — nada se descarta). Ficam aqui como material correlato, recuperável se um dia fizer sentido.

| Item | Por que saiu | Possível correlação |
|---|---|---|
| `stf-tema-1020` | É tema de **ISS** (imposto sobre serviços), não IPTU. O verbatim confirma. | Tributário municipal — se um dia o projeto abrir ISS. |
| `stj-resp-1658054` | **Previdenciário** (REsp 1.658.054/RS, DJe 29/06/2017, contribuições sobre verbas trabalhistas). Número não era do escopo. | Nenhuma com IPTU/TDC; mantido só para não perder o rastro. |

Para reativar um item: mover o par `.md`+`.json` de volta para `jurisprudencia/` e rodar `python3 scripts/consolidar.py`.
