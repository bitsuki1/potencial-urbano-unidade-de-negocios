# Solte aqui os CSV/JSON exportados pela extensão (evals/ground-truth/extensao-doc).
# A instância transforma cada linha num gabarito validado ao lado.

## Proveniência (NÃO PERDER)
- **Fonte bruta:** pasta do Google Drive do dono — folderId `1B6jlaQ12kl1T0iJiQPWGP7dbN4ayM6vU`
  (28 páginas HTML ARQUIP/DOSP do Diário Oficial, baixadas pela extensão do Claude em 2026-07-08).
- **Cópia durável no repo:** `gabaritos-doc-extraidos.{csv,json}` (extração) + `../gabaritos-tdc-doc.csv` (consolidado, dedup).
  O bruto NÃO está no repo (só no Drive); se precisar reconferir, é essa a pasta.
