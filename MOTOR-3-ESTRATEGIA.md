# MOTOR 3 — ESPACIAL (M-GEO) · Estratégia

> **Como foi produzida.** Loop de 7 lentes Sonnet + síntese/crítica Opus, limite de 5 rodadas. **NÃO atingiu triplo limpo — bateu no teto** (trajetória 10→5→2→4→1 ALTA).
> **Correções-chave que o loop provou (verificadas em arquivo):**
> - **G3 — o "maior buraco" (1.839 sem SQL) é ilusório:** só **63 são endereçáveis**; os outros **1.772 são a MESMA linha repetida** ("Luminárias Ornamentais da Light" — tombamento coletivo de postes, sem lote individual). 96% é modelagem de dado, não geocodificação. E o alvo do fuzzy (`iptu2026_cedentes.csv`) é um recorte já filtrado, não o cadastro completo.
> - **G2 — ZOE (77) tem regime próprio:** parâmetros por Quadro 2A/macroárea (PDE), não Quadro 3/zona-base — aplicar zona-base fabricaria CAbás. O Quadro 2A nunca foi ingerido.
> - **G4 — Decreto 57.536/16 (Regra da Esquina) nunca foi ingerido:** a citação central é não-verificada; há tensão com o MAT-5 (expor RANGE v_min/v_max em vez de MAX).
> - **G5 — `geo.lote.zona` em produção é coluna única**, não a N:N que o G5 propõe (e nem existe viva — ver crise de canonicidade do Motor 2).
> **Nota do escrutínio conjunto (ver `docs/ESCRUTINIO-CONJUNTO-MOTORES.md`):** o Drive tem `ZEPEC_BIR.shp` (geometria dos lotes) e `SIRGAS_SHP_LOTES` oficiais (59 distritos) que **substituem** os `LOTES_*_IA.csv` nossos (proibidos por D-DONO-4).

---

Four furos confirmed against the real files:
- q14 keyed by `(sq=block, codlog)`; `iptu2026_cedentes.csv` has one codlog per `sql_mestre` — no lot-face source (FURO #1).
- `rodar-evals.py` L59 globs `GT_DIR/*.json` unconditionally and calls `C.consultar(item["pergunta"])` with no dominio guard (FURO #2).
- `zepec_cedentes.csv` has NO `ca_basico`/`pcpt_m2`; those columns (the fabrication) live only in derived `zepec_cedentes_oficial.csv` (FURO #3).
- `consolidar.yml` has no `pip install` and no service container (FURO #4).

All four integrated into the definitive version below.

---

ESTRATÉGIA DO MOTOR 3 — ESPACIAL (M-GEO) — versão definitiva (2026-07-03)

Nota de síntese: esta versão fecha os quatro furos que sobreviveram a todas as rodadas — todos do gênero "prova que não prova". FURO #1: a Regra da Esquina (G4) não tem fonte de face NO NÍVEL DO LOTE; o MAX ingênuo sobre os codlogs do bloco é fabricação de falso-ALTO (viola 1.3), simétrica ao falso-zero que G1/G2 combatem — e ingerir o Decreto SOZINHO não desbloqueia um MAX correto. FURO #2: pôr `geo-overlay.json` dentro de `evals/ground-truth/` estoura `rodar-evals.py` (glob incondicional + `item["pergunta"]`), derrubando o gate de TODOS os domínios — a estratégia diagnosticava isso e reincidia no mesmo caminho. FURO #3: a fabricação `ca_basico`/`pcpt_m2` NÃO mora em `zepec_cedentes.csv` (não tem essas colunas) e sim no derivado `zepec_cedentes_oficial.csv`; um assert contra o arquivo NOMEADO lê 0 linhas → verde vacuoso enquanto `enriquecer_oficial.py` REGENERA a fabricação. FURO #4: G0 torna os DADOS reprodutíveis mas nunca provisiona as DEPENDÊNCIAS/serviços do CI (`shapely`, `pyshp`, `psycopg`, serviço PostGIS) — "roda no CI" segue inexecutável num runner limpo. Os demais elementos (disputa de área confinada a `in_q3`, exceção dual via overlay N:N, substring em `tipo_zepec`, conta de união reconciliada, fronteira RECEPTOR intocada) foram reconfirmados nos arquivos reais e permanecem.

---

(1) OBJETIVO DO MOTOR

Localizar cada tombado (lado cedente, já ZEPEC-BIR/APC declarada) no espaço e derivar DETERMINISTICAMENTE ZONA → CAbás → V como insumos que o Motor 5 consome, com a maior COBERTURA CORRETA possível e uma camada espacial REPRODUZÍVEL e AUDITADA por eval — hoje a única camada sem eval, o que viola 1.7. Doutrina que rege tudo: 1.3 (número/valor nasce no engine, nunca no LLM), 1.7 (citação por dispositivo), 1.5 (fonte única), 1.6 (vintage) e "declarei feito ≠ provei feito" (gate mecânico em `scripts/fechar-instancia.py` + `consolidar.yml`). Regra de ferro nos dois sentidos: onde a lei NÃO ampara, FLAG — nunca fabricar (falso-alto); onde a lei AMPARA a transferência, não zerar por regra frouxa (falso-zero). Cobertura RECEPTORA (OUC/PIU/ZER/estoque/OODC/`v_feed_alvos`) está FORA de escopo — lado comprador. Toda prova de hand-off deste motor aponta para o CSV CEDENTE (`zona_por_cedente.csv → enriquecer_oficial.py → zepec_cedentes_oficial.csv`), nunca para `v_feed_alvos`/`oodc.py`.

Fatos confirmados nos arquivos reais: `overlay_zona.py` monta UMA lista única de candidatos (zonas-base com CA e selos ZEPEC/ZOE lado a lado) e resolve o empate SÓ por preferência explícita (`if in_q3[i]: base_hit=i; break`, L88-94), nunca por área; usa `.centroid` (L77) e a 1ª feature por SQL; `DL` (L8) aponta para SCRATCHPAD efêmero de uma sessão (mesmo padrão em `gerar_xlsx.py` L8 `OUT`). `q14_cedentes_2025.csv` é chaveado por `(sq, codlog)` com `sq = sql[:6] = setor+quadra = o BLOCO/QUADRA`; `iptu2026_cedentes.csv` tem EXATAMENTE uma linha (um codlog) por `sql_mestre`. `enriquecer_oficial.py` L57 casa V por UMA face `(sql[:6], codlog)`. `zepec_cedentes.csv` NÃO possui colunas `ca_basico`/`pcpt_m2`; o derivado `zepec_cedentes_oficial.csv` possui `zona, ca_basico, pcpt_m2, saldo_pcpt_m2, preco_proxy_brl, …` — é ALI que a fabricação vive. `rodar-evals.py` faz `sorted(GT_DIR.glob("*.json"))` incondicional e chama `C.consultar(item["pergunta"], …)` sem guarda de schema/domínio. `consolidar.yml` roda scripts stdlib após `setup-python`, SEM `pip install` e SEM service container; `on.push.paths` cobre leis/jurisprudencia/scripts/evals/rag **e TAMBÉM `zepec/**`/`engines/**`/`tabelas/**`/`supabase/**`** (o "NÃO zepec/engines/tabelas" desta linha ENVELHECEU — corrigido no T2; nota da auditoria C-04, 2026-07-05). `tipo_zepec` tem 4 valores reais: 'tombado' (5.511), 'BIR/tombado' (425), 'BIR' (190), 'APC' (5). `zepec_cedentes.csv`: 32 linhas com `motivo_negociavel='vedado por lei (categoria AUE/APPa)'`. SRID oficial: SIRGAS2000 / UTM 23S (EPSG:31983).

---

(2) ENTREGAS

G0 — PRÉ-REQUISITO BLOQUEANTE E TRANSVERSAL: reprodutibilidade da fonte E provisionamento das dependências do CI
- O-que-faz: (i) tira o pipeline do scratchpad; (ii) — FURO #4 — provisiona as DEPENDÊNCIAS e SERVIÇOS que os scripts geo exigem, sem os quais nenhuma DoD "roda no CI" (G1/G5/G6) é executável num runner limpo. Entrega, nesta ordem:
  1. Parametrizar `DL` (`overlay_zona.py` L8) e `OUT` (`gerar_xlsx.py` L8) via config/variável de ambiente VERSIONADA no repo, nunca caminho de scratchpad de sessão. Assert: grep por `/tmp/claude`/`scratchpad` em `zepec/pipeline/**` = 0.
  2. Caminho de dados reprodutível, um dos dois por custo: (a) script determinístico de fetch dos shapefiles SIRGAS com checksum verificado (1.5/1.6), rodável A FRIO; OU (b) FIXTURE reduzido vendorizado (2-3 distritos contendo os 30-50 cedentes do ground-truth de G6), suficiente só para o eval, separado do dataset de produção, com pointer+checksum. Falha RUIDOSAMENTE (não silenciosa com 0 features) se o hash divergir.
  3. PROVISIONAMENTO DO RUNNER (FURO #4): adicionar ao `consolidar.yml` (a) um passo `pip install` explícito com as libs que os scripts geo importam — `shapely` (inclui `shapely.strtree`, `shapely.ops`), `pyshp` (`shapefile`) — versionadas em `requirements-geo.txt` com hash; (b) SE a via PostGIS de G5 for exercida no CI, um `services:` container Postgres+PostGIS e `psycopg`. Sem esse passo, `overlay_zona.py` e o eval geo dão `ModuleNotFoundError` no runner e o gate nunca chega a rodar.
- Por-que: é a diferença entre "declarei reprodutível/executável" e "provei". G0 sustenta materialmente as DoDs de "roda no CI" de G1/G5/G6, que hoje são decorativas.
- DoD mecânica: (a) `overlay_zona.py` roda num container SEM o scratchpad da sessão, lendo `DL` de config; (b) grep de `scratchpad`/`/tmp/claude` em `zepec/pipeline/**` = 0; (c) fetch/fixture com manifesto de checksum que falha ruidosamente no mismatch; (d) FURO #4 — `consolidar.yml` contém `pip install -r requirements-geo.txt` ANTES de qualquer passo geo, e um `python3 -c "import shapely, shapefile"` de fumaça passa no runner limpo; se G5-CI, o serviço PostGIS sobe e `import psycopg` passa.
- Prioridade: PRIMEIRO e absoluto.

G1 — Overlay lote×zona por ÁREA, disputa confinada às zonas-base do Quadro 3, com GATE de plausibilidade antes de unir features
- O-que-faz: substitui o join por `.centroid` (L77) por overlay por MÁXIMA ÁREA de interseção (max ST_Area lote×zona). Fallback sem polígono vencedor: `representative_point()` (ponto DENTRO do lote; resolve os 14 em L/côncavos), nunca `.centroid`. Expõe multi-zona: `zona_2`, `pct_area_zona1`, `flag_multi_zona`.
- REGRA CRÍTICA DO UNIVERSO DE DISPUTA: a comparação por MÁXIMA ÁREA compete SOMENTE entre camadas de zona-base (o conjunto `in_q3`, com CA no Quadro 3). Os selos ZEPEC_*/ZOE NUNCA entram nessa disputa — permanecem atributo paralelo (`tipo_zepec`/selo, tratado por G2), jamais candidatos ao campo `zona`. Motivo medido: ~2.098 dos ~3.986 cedentes com centroide (~53%) têm centroide caindo SIMULTANEAMENTE em zona-base com CA E em selo ZEPEC/ZOE; em amostra de 800 desses pares, em 262 (33%) a ÁREA do SELO é MAIOR (ex.: SQL 0090230017 base 213,0 m² vs selo 310,8 m²; SQL 0090110004 54,1 m² vs 227,9 m²). Disputa sobre lista unificada faria o SELO (sem CA) vencer por área em ~1/3 dos casos, regredindo lotes hoje corretos. Hierarquia: (1) vencedor por área DENTRO de `in_q3`; (2) só se nenhuma zona-base `in_q3` intersectar é que o lote fica sem `zona` e vai a G2.
- GATE DE PLAUSIBILIDADE antes de qualquer `unary_union` por SQL (o overlay atual pega arbitrariamente a 1ª feature). Dos 250 SQLs multi-feature: 188 homogêneos e 62 mixed-type em `lo_tp_lote`. Critérios:
  - (a) TIPO como SINAL, não veto: dos 62 mixed-type, os de distância máxima ≤15m são polígonos coincidentes (múltiplos cadastros do MESMO lote); a distribuição é contínua de 0m a 3.869m sem gap; nenhum arquivo documenta `lo_tp_lote` (grep=0). TIPO só bloqueia quando a DISTÂNCIA também ultrapassa o threshold.
  - (b) DISTÂNCIA: `geometry.distance` (não centroide) máxima entre pares; threshold ~15m → acima = COLISÃO de chave. Aplica-se a same-type E mixed-type.
  - (c) LOTE='0000': DOMINANTE mas DISJUNTO do universo mixed — dos 188 homogêneos, 187 têm todas as features com `lo_lote='0000'` (placeholder/agregado) e são SEMPRE bloqueados; dos 62 mixed-type, ZERO têm '0000'. O total bloqueado NÃO é "62 mixed + 187 '0000'".
  SQLs reprovados recebem `flag_sql_uniao_bloqueada=true` e ficam FORA do overlay automático até resolução por G3a — nunca fabricar a união.
- CONTA DE UNIÃO RECONCILIADA (contador único, sem par concorrente): medido contra os shapefiles reais, dos 62 mixed-type, 11 têm distância ≤15m (7 a 0,0m — 0010870001{F,M}, 0040530001{F,M}, 0872480001{F,M}, 0030030001{F,M}, 0260180001{F,V} — + 4 entre 10,7m e 14,8m: 0330070001, 0872530001, 3040560001, 0050870003), nenhum com '0000'. Somados ao único same-type não-'0000' (0361440003, 0m), a UNIÃO SEGURA recupera ~12 SQLs (1 + 11), NÃO ~0-1, NÃO ~7, NÃO 76-158. Bloqueio total ≈238 (187 same-type '0000' + ~51 mixed dist>15m).
- Por-que: corrige as 113 zonas erradas (88 com CAbás DIFERENTE = erro de VALOR) SEM reintroduzir erro por disputa contra selos nem por veto cego de tipo, com conta internamente consistente.
- DoD mecânica: (a) contador ÚNICO `N_SQLs_multi_feature_unificados` ≈12 (um só número); (b) contador SEPARADO `N_SQLs_uniao_bloqueada` ≈238 com decomposição em três baldes DISJUNTOS que somam exatamente 250 num único assert: {same-type '0000' ~187} + {mixed dist>15m ~51} + {recuperados ~12}, declarando que '0000' é disjunto do universo mixed (0 dos 62) e que os 11 mixed dist≤15m estão no balde RECUPERADO, não no bloqueado; (c) diff dos 113 SQLs que mudam de zona e 88 que mudam de CAbás, batendo com o eval de G6; (d) `metodo_overlay=area` e `pct_area_zona1` em 100% das linhas com geometria; (e) ASSERT DE REGRESSÃO DO UNIVERSO DE DISPUTA: 0 lotes onde o vencedor por área é selo ZEPEC_*/ZOE havendo zona-base `in_q3` intersectante; E, para os ~2.098 hits duplos hoje resolvidos por preferência, diff provando CAbás pós-G1 IDÊNTICO ao pré-G1 (não só os 113/88); (f) `flag_sql_uniao_bloqueada` nos ~238 bloqueados, assert de que nenhum recebeu zona/CAbás fechados e de que os ~12 recuperados receberam; (g) grep de `.centroid` = 0.

G2 — Zona-base sob o selo ZEPEC/BIR (os 454) — ZOE separado, vedação AUE/APPa por VARREDURA UNIVERSAL, exceção DUAL via APC OU BIR
- O-que-faz: overlay N:N devolvendo TODAS as camadas (o selo hoje "tampa" a zona-base), resolvendo os 454 sem CAbás (355 APC + 77 ZOE + 9 APP + 8 AUE + 5 BIR). Três destinos jurídicos:
  - (a) APC + BIR (≈360 lotes): elegíveis a TDC (Lei 16.050/2014 Art.65 I e Art.124 I — "ZEPEC-BIR e ZEPEC-APC poderão transferir o potencial construtivo básico definido em razão de sua localização"; ZEPEC sem CA próprio no Quadro 3; APC ancorada por Art.67 §4 "nas mesmas condições da ZEPEC-BIR"). Citação-base de que "o CAbás da zona-base se aplica ao lote" = Lei 16.050/2014 Art.124 I (chunk `rag/chunks/lei-municipal-saopaulo-16050-2014/131__art-124.json` ou `133__art-124.json`), NÃO "Lei 16.402/2016 Art.25". Definição técnica de CAbás por zona (Quadro 3) = Lei 16.402/2016 Art.56 I b + Art.58. Fluxo: zona-base de maior área → CAbásico do Quadro 3 SOMENTE após confirmação jurídica (Motor 4); senão `flag_ca_nao_confirmado=true`, nunca fabricar.
    - TERCEIRA CONDICIONANTE — manutenção dos atributos ZEPEC: Lei 16.402/2016 Art.24 §§1º-3º (leis/municipal-sp/…-16402-2016.md L979-994) condiciona A TRANSFERÊNCIA de qualquer imóvel ZEPEC (BIR e APC) à "recuperação e manutenção dos atributos que geraram o enquadramento", sob multa (Quadro 5); Lei 16.050/2014 Art.67 §4 reforça para APC ("atestado por parecer do órgão competente"). Sem campo no pipeline (grep 'parecer'/'conpresp'/'atributos que geraram' = 0). Adicionar `flag_manutencao_atributos_zepec_nao_verificada=true` aos ~360 enquanto não houver fonte (parecer CONPRESP/órgão, ou ausência de multa Quadro 5); Motor 5 trata pcpt_m2/preco_proxy_brl como PROVISÓRIO enquanto ativa.
  - (b) AUE + APP/APPa PUROS (sem dualidade elegível): TRANSFERÊNCIA LEGALMENTE VEDADA (Art.124 §2º da Lei 16.050/2014; sem remissão tipo Art.67 §4). "Qual CAbás" é IRRELEVANTE — a cessão é proibida.
    - VARREDURA UNIVERSAL sobre os 6.131 (não só o bloco-454): `zepec_cedentes.csv` tem 32 linhas com `motivo_negociavel='vedado por lei (categoria AUE/APPa)'`. FURO #3 — CORREÇÃO DE ARTEFATO: a fabricação de VALOR (`ca_basico`/`pcpt_m2` preenchidos) NÃO está em `zepec_cedentes.csv` (esse arquivo NÃO tem essas colunas); está no DERIVADO `zepec_cedentes_oficial.csv`, que `enriquecer_oficial.py` REGENERA a cada run (ele hoje não tem guarda de vedação). Portanto: o GUARD de vedação deve ser implementado DENTRO de `enriquecer_oficial.py` (zerar/omitir `zona`/`ca_basico`/`pcpt_m2`/`saldo_pcpt_m2`/`preco_proxy_brl` e gravar `flag_tdc_vedado_art124_par2=true`), e o ASSERT de regressão deve rodar contra `zepec_cedentes_oficial.csv` (o arquivo REGENERADO), não contra `zepec_cedentes.csv` — senão o assert lê 0 linhas com `ca_basico` → verde vacuoso enquanto a fabricação persiste no oficial. Regra: todo cedente cuja linha em `zepec_cedentes.csv` cite AUE/APPa (as 32), OU cujo overlay N:N devolva ZEPEC_AUE/ZEPEC_APP-puro SEM camada BIR/APC concorrente, sai do denominador com a flag.
    - EXCEÇÃO DUAL ANTES DE ZERAR — pela via APC OU BIR, decidida pelo OVERLAY N:N, não por string de selo nem por igualdade de `tipo_zepec`: um lote com motivo AUE/APPa MANTÉM elegibilidade e volta a (a) se o overlay N:N também devolver QUALQUER camada BIR (ZEPEC_BIR, ZEPEC_BIR_INDIC, ZEPEC_APP-BIR) — via BIR — OU QUALQUER camada APC (ZEPEC_APC) — via APC (Art.124 I, mesmos termos; Art.63 §ú confirma enquadramento múltiplo). Medido no overlay N:N real sobre os 17 sob selo AUE/APP: 4 SQLs duais APPa+APC elegíveis via APC (0070520000, 0070750000, 0070750029, 0070750176, todos `negociavel='sim'`) e 1 SQL geo-dual AUE+BIR (0111080001, `tipo_zepec` genérico 'tombado', sem shapefile combinado). `tipo_zepec` é SINAL FRACO corroborante: quando usado, por SUBSTRING (`'BIR' in tipo_zepec` / split por '/'), NUNCA por igualdade exata `=='BIR'` — senão as 425 linhas 'BIR/tombado' e o dual 0111080001 escapam da rede. Restauração NUNCA pela mera presença prévia de `ca_basico`.
  - (c) ZOE (77 lotes): NÃO recebe CAbásico do Quadro 3. Lei 16.402/2016 Art.15 §1º/§2º dá à ZOE regime PRÓPRIO (parâmetros por PIU/CTLU, coeficientes por MACROÁREA do Quadro 2A da Lei 16.050/2014), nunca extraído (grep "Quadro 2A"/"macroárea" em tabelas/ e engines/ = 0). `flag_zoe_aguardando_quadro2a=true`, `ca_basico` VAZIO, até (Motor 4/M-DADOS) o Quadro 2A ser extraído e cada ZOE mapeado à macroárea.
- Por-que: fechar zona/CAbás dos elegíveis é a maior alavanca; calcular valor para lotes com transferência proibida é fabricação (viola 1.3/1.7); MAS zerar um APPa+APC ou AUE+BIR dual é o falso-zero simétrico, na população que G2a resolve.
- DoD mecânica: (a) por elegível APC/BIR: `zona_base`, `pct_area_zona_base` e (`ca_basico` COM `citacao_confirmada=true` citando Lei 16.050/2014 Art.124 I OU `flag_ca_nao_confirmado=true`) + `flag_manutencao_atributos_zepec_nao_verificada` com Motor 5 tratando como provisório; (b) FURO #3 — assert rodado contra `zepec_cedentes_oficial.csv` (o arquivo regenerado, que TEM as colunas `ca_basico`/`pcpt_m2`): TODAS as AUE/APPa PURAS saem sem `ca_basico`/`pcpt_m2`/`saldo_pcpt_m2` fechados, com `flag_tdc_vedado_art124_par2=true`, fora do denominador; assert de regressão — nenhuma linha do oficial com `ca_basico` preenchido E `motivo_negociavel` citando AUE/APPa sobrevive ao build sem ser zerada/flagada (salvo dual comprovada); e o guard reside em `enriquecer_oficial.py`, provado por um teste que roda o script e reinspeciona o oficial; (b') assert de regressão da EXCEÇÃO DUAL: os 4 SQLs APPa+APC e o SQL AUE+BIR (0111080001) SAEM com CAbás/PCpt pela via APC/BIR (ou `flag_ca_nao_confirmado`), NÃO zerados; a detecção usa o overlay N:N, não o selo combinado nem `tipo_zepec`; as 425 linhas 'BIR/tombado' tratadas como candidatas à exceção por `'BIR' in tipo_zepec`; assert explícito de que exact-match não é usado em nenhum ponto de G2; (c) os 77 ZOE com `flag_zoe_aguardando_quadro2a=true` e `ca_basico` VAZIO (assert: nenhum ZOE com CAbás herdado); (d) contagem por tipo (APC/APP/AUE/BIR/ZOE + duais restaurados) reconciliada com 454 E com as 32 vedadas.

G3a — Geocodificar endereço → SQL do subconjunto REALMENTE endereçável por ponto único
- Motivo do recorte: dos 1.839 sem `sql_mestre`, só 63 têm `endereco_mestre`; 1.772 são a mesma linha repetida ("Luminárias Ornamentais da Light"); 1.825 carregam `sinais_revisar` citando 'sem lote individual na fonte'.
- O-que-faz: fuzzy `endereco_mestre` × cadastro IPTU (pg_trgm) + fallback espacial SIRGAS, SOMENTE nas linhas com `endereco_mestre` não-vazio, sem marca 'sem lote individual na fonte' E com número de porta parseável.
  - CANONICALIZAÇÃO OBRIGATÓRIA ANTES DO TRIGRAM (pré-requisito, sem ela o piso não é calibrável): `endereco_mestre` (de `montar_base.py::norm_endereco`, que EXPANDE via `TIPOS` — 'r'→'Rua', 'av'→'Avenida' — com acento) chega 'Rua Boa Vista, 293'; o lado IPTU (`filtro_iptu.py`, `end=f"{r[6]}, {r[7]}"`, sem tratamento; amostra `iptu2026_cedentes.csv` = 'R S CAETANO, 283', 'AV TIRADENTES, 84') chega abreviado/maiúsculo/sem acento. pg_trgm reduz sistematicamente o score do MESMO lote. Regra: uma canonicalização ÚNICA aos DOIS lados ANTES da similaridade — reutilizar/inverter o `TIPOS` de `montar_base.py` + `unaccent()`+`upper()`; o índice GIN opera sobre a string canônica, nunca a bruta.
  - FILTRO DE EXCLUSÃO POR SUBSTRING, não igualdade: `sinais_revisar` é composta (' | '). `'sem lote individual na fonte' not in sinais_revisar`. As que vazam com número parseável mas são bem coletivo/institucional (ex. 'Avenida Francisco Matarazzo, 455' = Parque Estadual F. Costa; 'Avenida Doutor Nullabraão Ribeiro, 313' = Fórum Criminal) recebem `flag_bem_coletivo_sem_lote=true` e NUNCA entram no matcher.
  - Denominador real ≈6, não 13: das 13 nominais, 7 não são lote único (3 rua+bairro sem número; 4 glebas em via longa). Sem número → `flag_gleba_sem_numero_nao_geocodavel=true` ou fallback espacial, nunca string-match. Célula com múltiplos números → candidata a MÚLTIPLOS SQLs, nunca 1 linha=1 SQL.
  - DESEMPATE POR DISTRITO: cruzar distrito do registro casado no IPTU_2026 contra `distrito` em zepec_cedentes.csv; divergência/não-confirmável → `flag_sql_candidato_nao_confirmado=true` mesmo com similaridade alta.
  - UNICIDADE/CARDINALIDADE: checar unicidade contra os SQLs já no CSV — nunca atribuir SQL que outra linha usa. Caso 'Alameda Rio Claro, 190' (4 linhas, 2 resolvidas, 2 cegas sem diferenciador) → INDETERMINADO, `flag_sql_candidato_nao_confirmado=true` para todas.
  - Piso de similaridade: abaixo dele flag; NUNCA sobrescreve SQL oficial.
- Pré-requisito bloqueante (Motor 2): lado direito = IPTU_2026.csv COMPLETO (3.920.972 linhas/894MB) em Postgres com pg_trgm+GIN sobre a string CANÔNICA — NÃO o recorte iptu2026_cedentes.csv.
- Hand-off: todo SQL recuperado é RE-SUBMETIDO ao overlay de G1/G2 antes de fechar (recuperar SQL ≠ recuperar CÁLCULO). Registrar em `zepec/oficial/funil.csv`.
- DoD mecânica: (a) denominador travado por assert com regra 'contains' (13 nominais, ≈6 endereçáveis 1:1); (b) 0 linhas escritas em sql_mestre sem número parseável 1:1; (c) regressão: 'Av. Francisco Matarazzo, 455' e 'Av. Dr. Nullabraão Ribeiro, 313' caem em `flag_bem_coletivo_sem_lote` e nunca entram no matcher; (d) unicidade: 0 sql_mestre duplicado; as 2+ cegas de 'Alameda Rio Claro, 190' ficam `flag_sql_candidato_nao_confirmado`; (e) conferência de distrito em 100% dos candidatos; (f) taxa de recuperação medida contra AMOSTRA humana, não auto-reporte do pg_trgm; (g) nº de SQLs recuperados que ganharam zona+CAbás no funil; (h) assert pré/pós-canonicalização: para ≥1 par conhecido-verdadeiro, provar que a canonicalização ELEVA o score de forma mensurável ANTES de fixar o piso.

G3b — Reclassificar o tombamento coletivo (não é geo)
- O-que-faz: marcar as ~1.776–1.825 linhas cuja `sinais_revisar` CONTENHA 'sem lote individual na fonte' (regra 'contains') com `flag_bem_coletivo_sem_lote=true` (NÃO_APLICÁVEL a overlay/PCpt/ZONA/CAbás); removê-las da meta e da narrativa "1.839". O matcher de G3a nunca roda sobre linha com esse flag.
- DoD mecânica: (a) contagem flagada reconciliada com 'contains'; (b) 0 linhas com o flag têm sql_mestre gerado por G3a; (c) meta recalculada sobre denominador elegível.

G4 — Regra da Esquina — REBAIXADA a RANGE (v_min/v_max), sem MAX, por AUSÊNCIA de fonte de face no nível do lote
- FURO #1 — reenquadramento estrutural: `q14_cedentes_2025.csv` é chaveado por `(sq, codlog)` com `sq = sql[:6] = a QUADRA/BLOCO`; os codlogs de um `sq` são os logradouros que delimitam a QUADRA, COMPARTILHADOS por todos os lotes do bloco — NÃO as testadas de UM lote. `iptu2026_cedentes.csv` tem exatamente UM codlog por `sql_mestre`; as OUTRAS testadas do lote não existem em lugar nenhum do join cadastral. NÃO existe fonte de adjacência lote→testada→logradouro no repo (grep por testada/face/adjacency só acha prosa em lei/doc). Consequência: o Decreto 57.536/2016 Art.3º IV define V = MAX entre as testadas DO LOTE, mas "V = MAX entre TODAS as faces do lote" só consegue enumerar os codlogs do BLOCO. Tomar MAX sobre eles atribui o V-máximo-do-bloco a todo lote interno de face única = SUPERAVALIAÇÃO em massa, o falso-ALTO simétrico ao falso-zero que G1/G2 policiam, violação DIRETA de 1.3 sobre a própria citação invocada. Ingerir o Decreto SOZINHO NÃO desbloqueia um MAX correto — falta o passo espacial que diz quais ARESTAS do polígono do lote tocam quais logradouros.
- O-que-faz (default e único caminho hoje): expõe o RANGE `v_min_sq`, `v_max_sq`, `v_face_usada` (a face única do join atual) e `flag_v_sensivel`, com `flag_citacao_nao_confirmada=true` por default. NUNCA promove a um número MAX. Também a recuperação das ~25 linhas sem V é doutrinariamente frágil: casar por QUALQUER codlog do bloco CHUTA uma testada que o lote pode não ter — só é lícito onde o bloco tem uma única face (v_min==v_max) ou por confirmação espacial; caso contrário, `flag_v_ausente_nao_recuperavel=true`, nunca herdar um V de face que talvez não seja do lote.
- DUPLO BLOQUEIO para promover a MAX (ambos necessários, o Decreto não basta): (i) Motor 4 ingere e confirma VERBATIM o Decreto 57.536/2016 Art.3º IV (grep verbatim em leis/ e rag/chunks/ hoje = 0); E (ii) M-GEO/M-DADOS define e cita uma fonte de ADJACÊNCIA lote-aresta→logradouro (interseção do contorno do polígono do lote SIRGAS com o eixo/segmento de logradouro), que hoje NÃO EXISTE. Só com ambos o MAX é computável e citável.
- Por-que: V é insumo DIRETO do preço-proxy (Motor 5). Codificar MAX sobre codlogs de bloco deslocaria o V de todo lote de esquina — e de interiores — para cima: fabricação de valor.
- DoD mecânica: (a) enquanto faltar QUALQUER dos dois bloqueios, 100% das linhas com `flag_citacao_nao_confirmada=true` e Motor 5 lê `v` como PROVISÓRIO (range), nunca fecha `v_face_usada` como número definitivo (assert no gate); (b) assert de que NENHUMA linha recebe V=MAX enquanto a fonte de adjacência lote-face não existir e for citada — o gate reprova se algum V for promovido só com o Decreto ingerido; (c) `v_min_sq`/`v_max_sq`/`v_face_usada` presentes; as ~25 sem V ou marcadas `flag_v_ausente_nao_recuperavel` ou recuperadas SÓ quando v_min==v_max no bloco; (d) após AMBOS os bloqueios, `citacao_v='Decreto 57.536/2016, Art.3º, IV'` + `fonte_adjacencia_face` citada, e só então MAX aplicado sobre as faces PROVADAMENTE do lote.

G5 — Hand-off da correção ao pipeline CEDENTE (CSV) + PostGIS opcional só para performance, com ALVO de carga declarado e verificado
- Reenquadramento: `geo.lote` está VAZIA (só DDL) e `enriquecer_oficial.py` lê CSV plano. O core de G5 é o HAND-OFF NO PIPELINE CEDENTE, não a materialização Postgres. Provar contra `v_feed_alvos`/`gerar_alvos.py` (motor OODC/RECEPTOR, fora de escopo) seria falso-verde estrutural.
- O-que-faz (core, em escopo): garantir que a saída corrigida de `overlay_zona.py` (G1) e a resolução de zona-base (G2) cheguem a `zepec/oficial/zona_por_cedente.csv` e, via `enriquecer_oficial.py`, a `zepec/ferramenta/zepec_cedentes_oficial.csv` com CAbás/pcpt_m2/preco_proxy_brl recalculados, PROPAGANDO as FLAGS de G1/G2/G4 (`flag_multi_zona`, `pct_area_zona1`, `flag_ca_nao_confirmado`, `flag_manutencao_atributos_zepec_nao_verificada`, `flag_zoe_aguardando_quadro2a`, `flag_citacao_nao_confirmada`, `flag_v_sensivel`, `flag_tdc_vedado_art124_par2`, `flag_sql_uniao_bloqueada`, `flag_sql_candidato_nao_confirmado`) como COLUNAS reais, para o Motor 5 tratar o valor como PROVISÓRIO/BLOQUEADO. Hoje `enriquecer_oficial.py` não tem coluna de flag — gap central a fechar (e é o MESMO arquivo onde o guard de vedação de G2b deve morar, FURO #3).
- O-que-faz (opcional, só se justificado por performance — o "JOIN <2s" que G3a precisa): materializar `geo.lote_zona` (N:N, GIST) em PostGIS, com guardrails:
  - ALVO DE CARGA DECLARADO E VERIFICADO: ANTES da carga, DECLARAR explicitamente o alvo — cidade inteira (~3,9M lotes / 96 SIRGAS_SHP_LOTES_*) OU cedente-only (~6.131 sql_mestre de zepec_cedentes.csv) — com justificativa de custo/benefício. Cedente-only é ordens de grandeza mais barato e é TUDO que o Motor 5/cedente consome; citywide só se houver consumidor EM ESCOPO que o exija (hoje não há). O passo "carregar 2-3 distritos, medir `pg_total_relation_size`, extrapolar" é AMOSTRA de orçamento, NÃO prova de carga.
  - GRANULARIDADE DA ZONA: `overlay_zona.py` L37-53 constrói UMA geometria por rótulo via `unary_union` (ZC = 6.750 polígonos unidos). Materializar assim dá ~46-90 linhas gigantes; o GIST não poda. Carregar a zona por FEIÇÃO ORIGINAL (uma linha por polígono), OU `ST_Subdivide` antes de indexar. Não afeta o overlay por área de G1, só o GIST.
  - Guardrail de FK (lado cedente): `geo.lote.zona` tem FK para `quadro3_ca_zona(zona)` e 'ZOE'/'ZEP' não existem lá. Migration que insere uma linha por zona-base ausente (mín. 'ZOE'), com ca_min/ca_basico/ca_max explicitamente NULL e comentário SQL citando ausência doutrinária — nunca valor numérico.
  - Orçamento 8GB — MEDIR ANTES de simplificar: 2-3 distritos sem simplificação, medir `pg_total_relation_size`, extrapolar; só se necessário `ST_SimplifyPreserveTopology` com tolerância AMARRADA ao eval geo-overlay de G6. SRID EPSG:31983 em tudo. (Fica FORA de G5 o INNER→LEFT em `v_feed_alvos`: pertence à cadeia OODC/RECEPTOR — backlog do outro motor.)
- Por-que: o número em escopo (PCpt/preço cedente) mora no CSV; a materialização Postgres é aceleração legítima, não o feed do cálculo cedente; "cidade inteira" é custo não-justificado sem consumidor em escopo.
- DoD mecânica: (a) HAND-OFF EM ESCOPO — ponta-a-ponta: rodar `overlay_zona.py` corrigido (com o `DL` de G0) + `enriquecer_oficial.py` e provar por diff linha-a-linha contra o baseline pré-G1 que os 88 SQLs com CAbás divergente aparecem CORRETOS e com `pcpt_m2`/`preco_proxy_brl` recalculados no CSV oficial; (b) PROPAGAÇÃO DE FLAG — injetar no ground-truth um lote com cada flag ativa e provar que chega ao CSV oficial AINDA FLAGADO e que Motor 5 NÃO fecha número; (c) SE materialização PostGIS: (i) DECLARAR o alvo (citywide OU cedente-only) com justificativa registrada; (ii) ASSERT reconciliando carga REAL contra o alvo — cedente-only: `count(distinct sql_mestre em geo.lote) == count(distinct sql_mestre em zepec_cedentes.csv)` menos os sem geometria (listados); citywide: `>= 95%` da soma de features dos 96 shapes; geometria por feição/`ST_Subdivide` (assert nº linhas de zona >> 90); `EXPLAIN ANALYZE` provando poda GIST; `pg_total_relation_size` medido; migration placeholder ZOE/ZEP (ca NULL) com assert de mutação; loader idempotente (2x = mesmo estado); benchmark JOIN <2s marcado como infra, NÃO prova de hand-off, SEM tocar `v_feed_alvos`.

G6 — Eval da camada espacial (a ÚNICA sem eval) + gate que MORDE — com runner PRÓPRIO FORA do GT_DIR do RAG
- FURO #2 — CORREÇÃO ESTRUTURAL DO CAMINHO: `rodar-evals.py` L59 faz `sorted(GT_DIR.glob("*.json"))` INCONDICIONAL e L27 chama `C.consultar(item["pergunta"], …)` sem guarda de domínio. Um `geo-overlay.json` com schema geo (`sql_mestre, zona_esperada, …`, sem `pergunta`/`id`) colocado em `evals/ground-truth/` faz o runner RAG dar KeyError e DERRUBAR o gate de TODOS os domínios — exatamente a falha que a prosa diagnostica. Solução (uma das duas, obrigatória): (i) colocar o ground-truth geo FORA de `GT_DIR` — em `evals/ground-truth-geo/` — de modo que o glob do RAG nunca o veja; OU (ii) ensinar `rodar-evals.py` a PULAR arquivos sem `pergunta`/com `dominio!='rag'`. Adota-se (i) por menor acoplamento; o runner geo é separado.
- O-que-faz: validação da camada espacial contra amostra GeoSampa (30-50 cedentes conferidos à mão), com RUNNER e SCHEMA PRÓPRIOS, sobre o fixture/fetch reprodutível de G0 e as dependências provisionadas de G0/FURO #4.
  - (a) `evals/ground-truth-geo/geo-overlay.json` (FORA de `evals/ground-truth/`) com schema próprio: `sql_mestre, zona_esperada, ca_basico_esperado, fonte=GeoSampa`, mais os campos de V que G4 expõe: `v_min_esperado`, `v_max_esperado`, `v_face_usada_esperado`, `flag_v_sensivel_esperado`, `flag_citacao_nao_confirmada_esperado`. Inclui ≥1 caso de CADA classe de flag: multi-zona, ca_nao_confirmado, manutencao_atributos_nao_verificada, zoe_aguardando, tdc_vedado_art124, sql_uniao_bloqueada, sql_candidato_nao_confirmado, um lote de ESQUINA exercitando `flag_v_sensivel`/`flag_citacao_nao_confirmada`, E ≥1 lote DUAL restaurado (um dos 4 SQLs APPa+APC ou o 0111080001 AUE+BIR).
  - (b) script NOVO `evals/rodar-eval-geo.py` que roda a pipeline (`overlay_zona.py` + `enriquecer_oficial.py`; e a view PostGIS de G5 se existir) e faz assert campo-a-campo, SEM importar/depender de `rodar-evals.py`.
  - (c) step SEPARADO no `consolidar.yml` chamando esse script — executável só porque G0 tornou a fonte reprodutível E instalou `shapely`/`pyshp` (e PostGIS se G5-CI) no runner (FURO #4).
  - (d) EXTENSÃO dos triggers: `on.push.paths` cobre hoje leis/jurisprudencia/scripts/evals/rag — NÃO `zepec/**`. Adicionar `zepec/**` (e idealmente `engines/**`, `tabelas/**`), senão um push que reverta área→centroide, re-hardcode o scratchpad, reintroduza o exact-match de `tipo_zepec`, ou regenere a fabricação em `_oficial.csv` NÃO dispara o workflow (verde por OMISSÃO).
- Por-que: sem eval a zona nunca foi validada (viola 1.7); sem os paths o gate é cosmético; sem G0/dependências o gate nem roda; e um geo-json no GT_DIR do RAG derruba tudo (FURO #2).
- DoD mecânica: (a) `rodar-eval-geo.py` roda no CI (sobre fixture+deps de G0) e passa contra os 30-50; (b) MUTAÇÃO de valor: injetar zona/CAbás errado e o build FALHA; (c) MUTAÇÃO de flag (vedação): remover indevidamente `flag_tdc_vedado_art124_par2` de um AUE/APPa PURO e o build FALHA; (c') MUTAÇÃO da exceção dual: (i) forçar `flag_tdc_vedado_art124_par2` sobre um dual APPa+APC/AUE+BIR e o build FALHA; (ii) trocar a checagem de `tipo_zepec` de substring para exact-match e o eval FALHA nas 425 'BIR/tombado'/no dual 0111080001; (d) MUTAÇÃO de flag (V): enquanto `flag_citacao_nao_confirmada` ativa, Motor 5 NÃO fecha `v_face_usada` — E, FURO #1, assert de que NENHUM V=MAX é promovido sem a fonte de adjacência lote-face citada (não basta o Decreto); (e) MUTAÇÃO do universo de disputa: selo com área maior que a zona-base intersectante → eval FALHA se `zona` sair como selo; (e') MUTAÇÃO da conta de união: o assert de decomposição de G1 FALHA se a soma dos três baldes ≠ 250, ou se um mixed dist≤15m for contado como bloqueado, ou se o recuperado sair ~0-1/~7 em vez de ~12; (f) ground-truth inclui ≥1 zona DISPERSA (ZEIS-1/ZC/ZER) e — SE G5-PostGIS existir — afere que o tempo de junção não degenera; (g) grep em `consolidar.yml` confirma `zepec/**` nos paths; (h) FURO #2 — assert de que `evals/ground-truth/` NÃO contém nenhum arquivo geo (o geo vive em `evals/ground-truth-geo/`) e que `rodar-evals.py` (RAG) segue passando INTACTO no CI.

REJEITADO por gold-plating: (1) redesenhar o overlay de G1 para consumir feição-por-feição em Python — para ÁREA a união agregada dá o mesmo; granularidade só importa no GIST (G5). (2) Geocodificar os 1.772 "Luminárias da Light" — bem coletivo sem lote (G3b), zero retorno. (3) Consertar o INNER JOIN de `v_feed_alvos` — infra do motor RECEPTOR/OODC, fora de escopo. (4) Calibrar finamente o threshold >15m de G1 — baixo valor: '0000' domina o same-type e o corte mixed é ~15m. (5) Materializar a CIDADE INTEIRA em PostGIS sem consumidor em escopo — custo ordens de grandeza acima do cedente-only. (6) Tentar computar V=MAX ingerindo só o Decreto — INEXEQUÍVEL sem a fonte de adjacência lote-face (FURO #1); enquanto ela não existir, G4 fica no RANGE.

---

(3) PRIORIDADE INTERNA

G0 → G1 → G2 → G4 → G5 → G3a → G6, com G3b e as dependências de Motor 4/M-DADOS disparadas EM PARALELO (bloqueios externos de longo lead: ingestão do Decreto 57.536/2016 Art.3º IV E definição de fonte de adjacência lote-face para G4; extração do Quadro 2A para os 77 ZOE; confirmação da vedação Art.124 §2º para AUE/APPa PUROS; confirmação da elegibilidade APC via Art.124 I para a exceção dual; confirmação Art.24 §§1-3 da 16.402 + Art.67 §4 da 16.050 para a manutenção de atributos).
- G0 primeiro e absoluto: sem parametrizar `DL`, prover fetch/fixture E instalar shapely/pyshp/(psycopg) no runner (FURO #4), TODAS as DoDs "roda no CI" são inexecutáveis.
- G1: corrige o erro de VALOR de maior massa (88 CAbás) com a disputa confinada a `in_q3` e a conta de união RECONCILIADA (~12 recuperados, ~238 bloqueados; NÃO 76-158, NÃO ~0-1, NÃO ~7).
- G2: fecha ≈360 APC/BIR (provisórios sob `flag_manutencao_atributos`), RETIRA os vedados AUE/APPa PUROS por VARREDURA UNIVERSAL com o guard e o assert no arquivo CERTO — `enriquecer_oficial.py`/`zepec_cedentes_oficial.csv` (FURO #3), não `zepec_cedentes.csv` — E RESTAURA os duais via overlay N:N. Risco elevado: apaga fabricação viva E o falso-zero simétrico.
- G4 sobe (antes de G5): duplo bloqueio (citação verbatim + fonte de adjacência lote-face, FURO #1); abrir os pedidos a Motor 4/M-DADOS cedo; código expõe só o RANGE provisório.
- G5 antes de G3a: o hand-off CSV corrigido torna o re-overlay de G3a barato.
- G3a DESCE (universo real ≈6, com canonicalização + desempate de distrito + unicidade + 'contains' — retorno marginal, alto risco de falso-positivo).
- G3b — reclassificação a qualquer momento.
- G6 por último no fechamento, mas o ground-truth GeoSampa é coletado em paralelo desde G1 — e vive em `evals/ground-truth-geo/`, nunca em `evals/ground-truth/` (FURO #2).

META de cobertura: a alavanca REAL é G1 (88 CAbás; ~238 a `flag_sql_uniao_bloqueada`, ~12 recuperados) + G2 (≈360 APC/BIR com citação Art.124 I, provisórios sob `flag_manutencao_atributos`; + duais restaurados; +77 ZOE só após Quadro 2A; −32 AUE/APPa PUROS por vedação, incluindo os hoje indevidamente calculados no `_oficial.csv`) + G4 (RANGE v_min/v_max exposto; MAX só após o duplo bloqueio). Meta medida FIM-A-FIM (zona+CAbás+PCpt no `funil.csv`/CSV oficial), sobre denominador ELEGÍVEL = 6.131 − ~1.825 bens coletivos ('contains') − 32 vedados Art.124 §2º (menos os duais restaurados) − `flag_sql_uniao_bloqueada` não resolvidos (~238). O número honesto é MENOR que o "≥3.400" da v1 porque aquele contava fabricação e união cega — mas NÃO subtrai os duais que a lei elegibiliza, que voltam ao numerador.

---

(4) FRONTEIRAS COM OUTROS MOTORES

- Motor 2 (M-DADOS): tabelas/loaders base com vintage em Postgres — IPTU_2026.csv completo (894MB, pré-req de G3a) indexado sobre string CANÔNICA; shapefiles SIRGAS (incl. `lo_tp_lote`, o padrão `lote='0000'` do gate de G1, e as camadas ZEPEC_APC/ZEPEC_BIR/ZEPEC_APP-BIR que o overlay N:N de G2 consome) com fetch/checksum ou fixture (fronteira com G0); Q14, Quadro 3; a extração do Quadro 2A (Lei 16.050/2014) para ZOE; E — FURO #1 — a definição/produção de uma FONTE DE ADJACÊNCIA lote-aresta→logradouro (interseção contorno-do-lote × eixo de logradouro) sem a qual o MAX de G4 é incomputável. O overlay CONSOME essas tabelas; não as produz.
- Motor 4 (M-JUR): ingestão verbatim + chunk RAG do Decreto 57.536/2016 Art.3º IV (um dos DOIS bloqueios de G4 — o outro é a fonte de adjacência, que NÃO é jurídico e sim de dados/espacial); confirmação de que o CAbás da zona-base se aplica a APC via Lei 16.050/2014 Art.124 I + Lei 16.402/2016 Art.56 I b/Art.58 (bloqueio de G2a e base da EXCEÇÃO DUAL via APC — NÃO Art.25 da 16.402); Art.63 §ú (enquadramento múltiplo, base da dualidade); vedação Lei 16.050/2014 Art.124 §2º para AUE/APPa PUROS (bloqueio de G2b); condicionante de manutenção dos atributos — Lei 16.402/2016 Art.24 §§1º-3º + Lei 16.050/2014 Art.67 §4 (bloqueio da promoção a definitivo dos ~360). Citação é entregável de Motor 4.
- Motor 5 (M-CALC): consome zona/CAbás/V do lado CEDENTE via `zona_por_cedente.csv → enriquecer_oficial.py → pcpt.py` (Art.125) — NÃO via `v_feed_alvos`/`oodc.py`. G5 garante que a correção de G1/G2/G4 chega ao CSV com flags propagadas; enquanto qualquer flag de não-confirmação/manutenção/ZOE-aguardando/vedação/V-provisório estiver ativa, Motor 5 trata como provisório ou nunca calcula (vedados) e não fecha número. `v_feed_alvos`/`gerar_alvos.py`/`oodc.py` é RECEPTOR — nenhum item de M-GEO o toca.
- Motor 1 (M-GATE): step de eval geo no `consolidar.yml` + extensão dos paths (`zepec/**`) + testes de mutação (valor, universo de disputa, conta de união, exceção dual APC/BIR, exact-match de `tipo_zepec`, flag de vedação, flag de V, e — FURO #1 — a proibição de V=MAX sem fonte de adjacência) — o gate mecânico que faz "provei feito" valer, viável só porque G0 tornou a fonte reprodutível E instalou as dependências no runner (FURO #4). O runner geo é PRÓPRIO e o ground-truth vive FORA de `GT_DIR` para não quebrar o gate RAG (FURO #2). Segurança (travas de escrita em `escritorio-do-mou` e DEV keepee) = Motor 1.

---

(5) O QUE FICA PRONTO QUANDO O MOTOR FECHA

- Cada tombado cedente com geometria tem `zona` derivada por ÁREA (não centroide), confinada às zonas-base do Quadro 3, com multi-zona exposta e as 113 zonas/88 CAbás errados corrigidos e provados por diff contra o eval GeoSampa.
- Os 454 sob selo resolvidos por destino jurídico: ≈360 APC/BIR com CAbás citado (Art.124 I) e provisórios sob `flag_manutencao_atributos`; 77 ZOE em espera explícita do Quadro 2A; AUE/APPa PUROS zerados/flagados por vedação (Art.124 §2º) NO ARQUIVO REGENERADO (`_oficial.csv`), com a fabricação de valor apagada na fonte (`enriquecer_oficial.py`) e provada por assert no arquivo certo; e os duais APPa+APC / AUE+BIR restaurados via overlay N:N.
- O pipeline espacial é REPRODUZÍVEL a frio num runner limpo (sem scratchpad, com fetch/fixture verificado por checksum E com shapely/pyshp/psycopg instalados) — nenhuma DoD "roda no CI" é decorativa.
- V entregue como RANGE auditável (v_min/v_max/v_face_usada/flag_v_sensivel), NUNCA promovido a MAX enquanto faltar a fonte de adjacência lote-face E a citação verbatim do Decreto — falso-alto impossível de escapar pelo gate.
- Todas as flags (`flag_multi_zona`, `flag_ca_nao_confirmado`, `flag_manutencao_atributos_zepec_nao_verificada`, `flag_zoe_aguardando_quadro2a`, `flag_citacao_nao_confirmada`, `flag_v_sensivel`, `flag_tdc_vedado_art124_par2`, `flag_sql_uniao_bloqueada`, `flag_sql_candidato_nao_confirmado`, `flag_bem_coletivo_sem_lote`) são COLUNAS reais que chegam ao CSV oficial e travam o Motor 5 no provisório/bloqueado.
- A camada espacial deixa de ser a ÚNICA sem eval: há ground-truth GeoSampa (30-50), runner próprio FORA do GT_DIR do RAG, step no `consolidar.yml` com `zepec/**` nos paths e testes de mutação que MORDEM em valor, vedação, exceção dual, universo de disputa, conta de união e promoção indevida de V — com o gate RAG (`rodar-evals.py`) seguindo intacto.
- Meta de cobertura recalculada HONESTAMENTE sobre denominador elegível, sem fabricação e sem falso-zero, com o re-overlay de SQLs recuperados por G3a barato porque o hand-off CSV já está correto. Zero item, prova ou consumidor do lado comprador.